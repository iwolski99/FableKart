{
  "name": "fablekart",
  "version": "1.0.0",
  "description": "A browser-based 3D kart racing game built with Three.js. Procedural tracks, karts, textures and sound — no external assets.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
